# Copilot Instructions

This is a Langflow (visual LLM workflow builder) template for StackBlaze.

## When checking for updates
- Check if langflowai/langflow Docker image has newer stable tags
- Verify environment variable names match current version
- Check for new required configuration
- Keep the template minimal — deployment-ready